app = "lgchatbotsr"  # Уникальное имя приложения
primary_region = "ams"  # Выберите регион (ams = Амстердам)

[build]
  dockerfile = "Dockerfile"

[env]
  PORT = "8080"
  API_TOKEN = "7731278147:AAGNBi8Td-kSWr0Hhxdh0r46fXKzVsI0S2w"
  ADMIN_PASSWORD = "LegerisKEY-738197481275618273858173"

[vm]
  memory = "512mb"  # Увеличиваем RAM для aiogram

[[services]]
  internal_port = 8080
  protocol = "tcp"
  auto_stop_machines = false
  auto_start_machines = true
  min_machines_running = 1
  processes = ["app"]
  [[services.ports]]
    handlers = ["http"]
    port = 80
  [[services.ports]]
    handlers = ["tls", "http"]
    port = 443

[[services]]
  internal_port = 8080
  protocol = "tcp"
  auto_stop_machines = false
  auto_start_machines = true
  min_machines_running = 1
  processes = ["worker"]
  [services.concurrency]
    hard_limit = 1
    soft_limit = 1
    type = "connections"
